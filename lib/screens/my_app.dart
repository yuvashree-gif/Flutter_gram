import 'package:flutter/material.dart';
import 'screens/home_page.dart'; // Import the HomePage file

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
